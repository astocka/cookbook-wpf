<Page x:Class="Cookbook.Home"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Cookbook"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Cookbook - Home">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="*" />
            <RowDefinition Height="60" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="1" >
            <Image Source="src/img/icons8-rice-bowl-100.png" VerticalAlignment="Center" Height="100"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <TextBlock FontSize="36" Foreground="{DynamicResource {x:Static SystemColors.ActiveCaptionBrushKey}}" VerticalAlignment="Center">COOK</TextBlock>
                <TextBlock FontSize="36" Foreground="{DynamicResource {x:Static SystemColors.WindowFrameBrushKey}}" VerticalAlignment="Center">BOOK</TextBlock>
            </StackPanel>
        </StackPanel>

    </Grid>
</Page>
